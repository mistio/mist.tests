import sys

# this is import is not used for it's code but rather to make sure that
# it's own imports are correct
import tests.gui.core.environment

if __name__ == '__main__':
    print "This is a dummy script. And it works!!"
    sys.exit(0)
